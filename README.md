# notion-web-hub (UI A / Style 1) - Final

## Run
```bash
npm install
cp .env.local.example .env.local
# edit .env.local and set NOTION_TOKEN
npm run dev
```

## Notes
- Notion DB must share the Integration with all 5 databases.
- Music player uses a hidden YouTube embed (no URL text is shown). Clicking Play starts playback.
